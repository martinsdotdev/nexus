// The overlay's virtual canvas size in CSS px. Both the overlay renderer and the
// editor canvas lay widgets out on this surface (scaled to fit), so the size is a
// single shared constant rather than a literal repeated per route.
export const VIRTUAL_W = 1920;
export const VIRTUAL_H = 1080;
