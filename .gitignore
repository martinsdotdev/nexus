# Rust build output
/target

# pnpm workspace root node_modules (per-package builds ignore their own)
node_modules/

# Local Claude Code state (not shared)
.claude/

# CLAUDE.md is a per-machine symlink to the canonical AGENTS.md; not tracked.
/CLAUDE.md
